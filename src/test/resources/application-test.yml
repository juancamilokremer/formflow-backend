spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/formflow_test}
    username: ${SPRING_DATASOURCE_USERNAME:formflow}
    password: ${SPRING_DATASOURCE_PASSWORD:formflow123}
  jpa:
    show-sql: true
  flyway:
    enabled: true

app:
  jwt:
    secret: ${JWT_SECRET:test-secret-para-ci-minimo-32-caracteres-ok}
    expiration-ms: 3600000
    refresh-expiration-ms: 86400000
